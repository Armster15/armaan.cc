---
type Props = {
  title: string;
  headingType?: "h1" | "h2" | "h3" | "h4" | "h5" | "h5";
  class?: string;
};

const { title, headingType: Heading = "h1", class: className } = Astro.props;
---

<Heading class:list={["text-2xl font-semibold", className]}>
  {title}
</Heading>
