---
interface Props {
  isNested?: boolean;
}

const { isNested } = Astro.props;
---

<ul
  class={isNested
    ? "ml-4 sm:ml-5 mt-2 space-y-4 sm:space-y-2"
    : "ml-2 sm:ml-3 space-y-4 marker:text-subtitle/70 sm:space-y-2 [&_ul>*]:marker:text-subtitle-light"}
>
  <slot />
</ul>
